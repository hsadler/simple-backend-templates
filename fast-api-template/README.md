# FastAPI template

## Getting started

Requirements:
- poetry
- httpie

install dependencies:

```sh
poetry install
```

run local server:

```sh
poetry run uvicorn src.main:app --reload
```

## Other dev commands:

enter poetry shell:

```sh
poetry shell
```